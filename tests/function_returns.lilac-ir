function @main
{
    allocate #bit32, %result
    address @get, &1
    allocate #bit32, %tmp:1
    call &1, out %tmp:1
    copy #bit32, %tmp:1, %result
    return
}

function @get
    out #bit32 %result
{
    copy #bit32, 0x1, &1
    copy #bit32, &1, %result
    return
}